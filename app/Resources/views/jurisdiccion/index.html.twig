{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
  <div class="row">
    <h1>Lista de Jurisdicciones</h1>
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for jurisdiccion in jurisdiccions %}
            <tr>
                <td><a href="{{ path('jurisdiccion_show', { 'id': jurisdiccion.id }) }}">{{ jurisdiccion.id }}</a></td>
                <td>{{ jurisdiccion.name }}</td>
                <td>
                    <ul class="list-unstyled">
                        <li>
                            <a class="btn btn-default" href="{{ path('jurisdiccion_show', { 'id': jurisdiccion.id }) }}">
                            <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span>
                            </a>
                            <a class="btn btn-default" href="{{ path('jurisdiccion_edit', { 'id': jurisdiccion.id }) }}">
                            <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
                            </a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


  </div>
  <ul class="list-inline">
      <li>
          <a class="btn btn-default" href="{{ path('jurisdiccion_new') }}"> Crear Nueva Jurisdicción </a>
      </li>
  </ul>

</div>
{% endblock %}
